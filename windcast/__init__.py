"""windcast — агентная система прогноза выработки ВЭС на 24–48 часов.

Модули:
    scada     — данные турбин: 10 мин → час, UTC, флаги простоев
    weather   — архив прогнозов погоды Open-Meteo (Previous Runs API)
    features  — признаки строго «на момент выпуска» прогноза
    models    — baseline, каскад (ветер → кривая мощности), калибровка, прямая модель (2-е мнение)
    backtest  — пошаговый бэктест по протоколу кейса
    agent     — граф агента: погода → проверка → прогноз → критик → пересчёт
"""

__version__ = "0.1.0"
