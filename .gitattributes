# Перевод строк фиксируется в репозитории: инструменты на Windows иначе
# незаметно переводят файлы в CRLF, а shell-скрипты внутри контейнера
# с CRLF просто не запускаются.
* text=auto eol=lf

*.png binary
*.jpg binary
*.pdf binary
*.ico binary
